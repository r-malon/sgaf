version: "2"
sql:
- engine: "sqlite"
  queries: "query/"
  schema: "schema/"
  gen:
    go:
      out: "db"
      sql_driver: "modernc.org/sqlite"
      emit_prepared_queries: true
      emit_json_tags: true
